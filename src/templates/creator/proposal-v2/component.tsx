// Renders identically to proposal-v1 — this template's public page doesn't
// need to differ, only its admin edit form (see `./form`) does.
export { ProposalV1 as ProposalV2 } from "../proposal-v1/component";
